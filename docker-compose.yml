version: "3"

services:
  backend:
    build: ./backend
    container_name: backend_cicd
    ports:
       - "5000:5000"
    
  frontend:
    build: ./frontend
    container_name: frontend_cicd
    ports:
      - "80:80"
    depends_on:
      - backend
