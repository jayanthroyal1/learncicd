server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index.html;
        try_files 4URL /index.html
    }

    location /api/ {
        proxy_pass http://backend:5000/;
    }
}